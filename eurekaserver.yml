server:
  port: 8070

eureka:
  instance:
    hostname: localhost
  client:
    fetchRegistry: false #eureka doesn't call other microservices
    registerWithEureka: false #don't register with yourself
    serviceUrl:
      defaultZone: http://${eureka.instance.hostname}:${server.port}/eureka/
